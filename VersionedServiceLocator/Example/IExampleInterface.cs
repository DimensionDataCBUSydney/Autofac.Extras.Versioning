namespace VersionedServiceLocator
{
	/// <summary>
	/// Our example (test) interface
	/// </summary>
	public interface IExampleInterface
	{
		/// <summary>
		/// Get an example method (just so we can see this works).
		/// </summary>
		/// <returns></returns>
		string GetMessage();
	}
}
